# 全局字体、标点与换行符规范 (Global Formatting & Line Break Rule)

> 本规范为全域及跨 Agent 通用的排版与格式输出标准。任何 Agent 的每个会话均须严格遵守。

## 1. 换行符与段落规范（新增强制标准）

- **统一使用硬回车 (Paragraph Break ↵)**：
  - Word 文档生成与排版中，段落之间的换行统一使用标准的**硬回车（段落标记 `↵`）**。
  - **严禁**在 Word 文档中使用向下小箭头的**软回车（Line Break `↓` / 手动换行符）**。

## 2. 字体配置规范

- **中文文字**：统一强制使用 **宋体 (SimSun)**。
- **英文与数字**：统一强制使用 **Times New Roman (新罗马字体)**。
- **适用场景**：所有导出的 Word 文档（.docx）、HTML 页面、报告及文本生成。

## 3. 标点符号规范

- **中文双引号与单引号（强制执行）**：
  - 中文正文中的引用、强调、概念、专有名词与对话，统一强制使用全角中文弯双引号 **“** 与 **”**。
  - 嵌套引用统一使用全角中文弯单引号 **‘** 与 **’**。
  - **严禁**在中文普通文本中混用英文半角直引号 `"` 或 `'`。

- **全角中文标点正规化**：
  - 中文正文统一使用全角标点：中文冒号 **：**、中文逗号 **，**、中文句号 **。**、中文顿号 **、**、中文括号 **（）** 与中文书名号 **《》**。

## 4. 特别例外规定

- **文末参考文献（References / 参考文献列表）**：
  - 文末参考文献的标点符号严格例外，统一使用学术格式规范（如 GB/T 7714 / APA 格式）中的**半角/英文标点符号**（英文点 `.`，半角逗号 `,`，半角冒号 `:`，英文括号 `()`）。
- **技术代码与命令**：
  - 代码块（``` ）、内联代码（`...`）、文件路径、URL 链接、Shell 指令中保留英文原生半角符号。
